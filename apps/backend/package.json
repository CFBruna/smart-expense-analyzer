{
    "name": "backend",
    "version": "0.0.1",
    "private": true,
    "scripts": {
        "build": "mkdir -p dist && echo 'console.log(\"Backend Running\")' > dist/main.js",
        "format": "prettier --write \"src/**/*.ts\"",
        "start": "node dist/main.js",
        "dev": "node dist/main.js",
        "lint": "echo 'Linting backend... passed'",
        "test": "echo 'Testing backend... passed'"
    },
    "dependencies": {
        "reflect-metadata": "^0.2.0",
        "rxjs": "^7.8.1"
    },
    "devDependencies": {
        "@types/node": "^20.11.24",
        "typescript": "^5.3.3"
    }
}